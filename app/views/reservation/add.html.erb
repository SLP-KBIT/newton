<h1>予約ページ</h1>

<p>
個数と借りたい日を指定してください。
</p>

<%= form_tag(reservation_create_path, method: :post) do %>
<table class="table table-bordered">
    <thead>
      <th>物品名</th>
      <th>個数</th>
      <th>借りたい日</th>
      <th>最大貸出期間</th>
      <th>カテゴリ</th>
      <th>保管場所</th>
      <th>付属品</th>
    </thead>
    <tbody>
      <% @items.each_with_index do |item, i| %>
        <tr>
          <td><%= item.name  %></td>
          <td><%= select :item, item.id, @array2[i] %></td>
          <td>
            <%= text_field :start_date, item.id, id: :datepicker %>
          </td>

          <td><%= item.lending_period %></td>
          <td><%= item.category_text %></td>
          <td><%= item.place %></td>
          <td><%= item.attachments %></td>
	</tr>
      <% end %>
    </tbody>
  </table>
  <%= submit_tag '予約', name: 'reserve', class: 'btn' %>
<% end %>
