<h1>メッセージの作成</h1>
<stylesheet>
</stylesheet>

<%= form_for :message, url: { action: :add } do |f| %>
  <%= f.label :宛先 %>
  <%= f.text_field :name %>

  <%= f.label :件名 %>
  <%= f.text_field :subject %>

  <%= f.label :内容 %>
  <%= f.text_area :comtent, :size => "100x10" %>

  <br>
  <%= f.submit '送信', class: 'btn' %>
<% end %>
