<h1>カートページ</h1>

<p>
個数を指定してください。

個数を確認のうえ、「借りる」ボタンを押してください
</p>

<%= form_tag('/history/lend/add', method: :post) do %>
  <table class="table table-bordered">
    <thead>
      <th>物品名</th>
      <th>個数</th>
      <th>最大貸出期間</th>
      <th>カテゴリ</th>
      <th>保管場所</th>
      <th>付属品</th>
    </thead>
    <tbody>
      <% @items.each_with_index do |item, i| %>
        <tr>
          <td><%= item.name  %></td>
          <td><%= select :item, :amount, @array2[i] %></td>
          <td><%= item.lending_period %></td>
          <td><%= item.category %></td>
          <td><%= item.place %></td>
          <td><%= item.attachments %></td>
	</tr>
      <% end %>
    </tbody>
  </table>
  <%= submit_tag '借りる', name1: '借りる', class: 'btn' %>
<% end %>
